.game-container {
  position: relative;
  width: 100vw;
  height: 100vh;
  background: linear-gradient(180deg, #0a1a0a 0%, #152515 50%, #0a1a0a 100%);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* 顶部状态栏 */
.status-bar {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12rpx;
  padding: 12rpx 16rpx;
  background: linear-gradient(180deg, rgba(30, 60, 30, 0.95) 0%, rgba(15, 30, 15, 0.9) 100%);
  border-bottom: 3rpx solid rgba(80, 255, 80, 0.4);
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.5);
}

.status-main {
  flex: 1;
  min-width: 0;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 10rpx;
}

.status-actions {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  gap: 10rpx;
  padding-top: 2rpx;
}

.battle-banner {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  padding: 8rpx 16rpx 10rpx;
  background: linear-gradient(180deg, rgba(10, 22, 10, 0.92) 0%, rgba(6, 14, 6, 0.85) 100%);
  border-bottom: 2rpx solid rgba(80, 255, 80, 0.16);
}

.battle-banner-row {
  width: 100%;
  min-width: 0;
}

.battle-banner-row-top {
  white-space: nowrap;
}

.battle-banner-row-top .banner-chip {
  display: inline-flex;
  vertical-align: top;
  min-width: 152rpx;
  margin-right: 10rpx;
}

.battle-banner-row-top .banner-chip:last-child {
  margin-right: 0;
}

.battle-banner-row-detail {
  display: flex;
  gap: 8rpx;
}

.banner-chip {
  min-width: 0;
  max-width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 7rpx 15rpx;
  border-radius: 999rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.12);
  background: rgba(255, 255, 255, 0.05);
}

.banner-chip.active {
  border-color: rgba(255, 215, 120, 0.55);
  box-shadow: 0 0 18rpx rgba(255, 215, 120, 0.2);
}

.banner-chip-threat {
  border-color: rgba(255, 200, 120, 0.4);
  background: rgba(255, 180, 80, 0.08);
}

.banner-chip-objective {
  border-color: rgba(150, 255, 170, 0.22);
  background: rgba(120, 255, 150, 0.06);
}

.banner-chip-objective.ready {
  border-color: rgba(180, 255, 190, 0.62);
  box-shadow: 0 0 18rpx rgba(140, 255, 170, 0.18);
}

.banner-chip-command {
  border-color: rgba(130, 215, 255, 0.24);
  background: rgba(110, 200, 255, 0.07);
}

.banner-chip-command.ready {
  border-color: rgba(160, 236, 255, 0.62);
  box-shadow: 0 0 18rpx rgba(120, 220, 255, 0.18);
}

.banner-chip-chain {
  border-color: rgba(180, 220, 255, 0.36);
  background: rgba(110, 160, 255, 0.1);
}

.banner-chip text {
  display: block;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  font-size: 20rpx;
  color: #f7f5d0;
  font-weight: 600;
  white-space: nowrap;
}

.banner-detail-text {
  flex: 1 1 0;
  min-width: 0;
  display: block;
  font-size: 18rpx;
  line-height: 1.35;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  color: rgba(220, 255, 220, 0.75);
}

.banner-detail-text-secondary {
  color: rgba(200, 235, 255, 0.76);
}

.status-item {
  display: flex;
  align-items: center;
  gap: 6rpx;
  flex-shrink: 0;
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.4) 0%, rgba(0, 0, 0, 0.2) 100%);
  padding: 8rpx 14rpx;
  border-radius: 22rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.1);
  box-shadow: inset 0 2rpx 5rpx rgba(255, 255, 255, 0.05);
}

.status-item.wave {
  min-width: 112rpx;
}

.status-icon {
  font-size: 24rpx;
  margin-right: 0;
}

.status-value {
  font-size: 28rpx;
  font-weight: bold;
  color: #fff;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.5);
  white-space: nowrap;
}

.status-label {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.6);
  margin-left: 4rpx;
}

.gold-value {
  color: #ffd700;
  text-shadow: 0 0 15rpx rgba(255, 215, 0, 0.6), 0 2rpx 4rpx rgba(0, 0, 0, 0.5);
  min-width: 0;
  font-size: 30rpx;
}

.lives-value {
  color: #ff6666;
  text-shadow: 0 0 15rpx rgba(255, 102, 102, 0.6), 0 2rpx 4rpx rgba(0, 0, 0, 0.5);
}

.pause-btn,
.sound-btn {
  width: 56rpx;
  height: 56rpx;
  flex-shrink: 0;
  background: linear-gradient(135deg, rgba(80, 255, 80, 0.2) 0%, rgba(40, 120, 40, 0.3) 100%);
  border: 2rpx solid rgba(80, 255, 80, 0.5);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  box-shadow: 0 0 15rpx rgba(80, 255, 80, 0.3);
}

.sound-btn {
  margin-left: 0;
  margin-right: 0;
}

.sound-btn.muted {
  background: linear-gradient(135deg, rgba(120, 80, 80, 0.22) 0%, rgba(80, 45, 45, 0.3) 100%);
  border-color: rgba(255, 140, 140, 0.48);
  box-shadow: 0 0 14rpx rgba(255, 120, 120, 0.22);
}

.pause-btn:active,
.sound-btn:active {
  transform: scale(0.95);
  background: rgba(80, 255, 80, 0.3);
}

/* 游戏Canvas */
.battlefield-wrap {
  position: relative;
  width: 100%;
  flex: 1;
  min-height: 0;
  overflow: hidden;
}

.battlefield-wrap.prep-mode {
  flex: 1;
  min-height: 0;
}

.game-canvas {
  display: block;
  width: 100%;
  height: 100%;
  min-height: 0;
  background: linear-gradient(180deg, #0f1f0f 0%, #1a2a1a 50%, #0f1f0f 100%);
}

.game-canvas.prep-canvas {
  min-height: 0;
}

.prep-slot-layer {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.prep-slot {
  position: absolute;
  width: 54rpx;
  height: 54rpx;
  margin-left: -27rpx;
  margin-top: -27rpx;
  border-radius: 50%;
  border: 2rpx dashed rgba(220, 255, 220, 0.42);
  background: rgba(255, 255, 255, 0.05);
  pointer-events: auto;
}

.prep-slot.selectable {
  border-color: rgba(255, 215, 120, 0.8);
  background: rgba(255, 215, 120, 0.12);
}

.prep-slot.occupied {
  opacity: 0;
  pointer-events: none;
}

/* playing 时空塔位半透明显示（视觉参考，不可点） */
.prep-slot.readonly {
  opacity: 0.4;
  pointer-events: none;
}

/* 合成提示 */
.merge-hint {
  position: absolute;
  top: 35%;
  left: 50%;
  transform: translate(-50%, -50%);
  padding: 30rpx 60rpx;
  background: linear-gradient(135deg, rgba(255, 215, 0, 0.95) 0%, rgba(255, 170, 0, 0.95) 100%);
  border-radius: 30rpx;
  box-shadow: 0 0 50rpx rgba(255, 215, 0, 0.8), inset 0 2rpx 10rpx rgba(255, 255, 255, 0.3);
  animation: mergeHintPulse 0.5s ease-in-out infinite;
  z-index: 50;
}

.merge-text {
  font-size: 36rpx;
  font-weight: bold;
  color: #1a1a00;
  text-shadow: 0 2rpx 4rpx rgba(255, 255, 255, 0.3);
}

.merge-cost-sub {
  display: block;
  margin-top: 8rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: bold;
  color: #7a0000;
}

.merge-hint.merge-cost-warn {
  background: linear-gradient(135deg, rgba(255, 120, 120, 0.95) 0%, rgba(220, 60, 60, 0.95) 100%);
  box-shadow: 0 0 50rpx rgba(255, 80, 80, 0.8), inset 0 2rpx 10rpx rgba(255, 255, 255, 0.3);
}

.merge-hint.merge-cost-warn .merge-text {
  color: #2a0000;
}

@keyframes mergeHintPulse {
  0%, 100% { transform: translate(-50%, -50%) scale(1); box-shadow: 0 0 50rpx rgba(255, 215, 0, 0.8); }
  50% { transform: translate(-50%, -50%) scale(1.08); box-shadow: 0 0 70rpx rgba(255, 215, 0, 1); }
}

.drag-floating-tower {
  position: fixed;
  z-index: 60;
  pointer-events: none;
  transform: translate(-50%, -50%);
}

.drag-float-core {
  min-width: 88rpx;
  padding: 10rpx 14rpx 8rpx;
  border-radius: 24rpx;
  border: 3rpx solid rgba(255, 255, 255, 0.28);
  background: linear-gradient(180deg, rgba(8, 20, 12, 0.92) 0%, rgba(12, 34, 18, 0.9) 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2rpx;
}

.drag-float-emoji {
  font-size: 42rpx;
  line-height: 1;
}

.drag-float-level {
  font-size: 20rpx;
  font-weight: 700;
  line-height: 1.1;
  color: #ffe98a;
}

/* 底部仓库区域 */
.inventory-area {
  position: relative;
  background: linear-gradient(0deg, rgba(5, 15, 5, 0.98) 0%, rgba(20, 40, 20, 0.95) 100%);
  border-top: 3rpx solid rgba(80, 255, 80, 0.5);
  padding: 8rpx 10rpx 15rpx;
  box-shadow: 0 -8rpx 25rpx rgba(0, 0, 0, 0.5);
  display: flex;
  flex-direction: column;
}

.inventory-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  margin-bottom: 4rpx;
  padding: 0 10rpx;
  gap: 12rpx;
}

.inventory-actions {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 10rpx;
}

.inventory-title {
  font-size: 24rpx;
  color: #50ff50;
  font-weight: bold;
  text-shadow: 0 0 15rpx rgba(80, 255, 80, 0.5);
}

.supply-drop-btn {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #92ecff 0%, #4ab4ff 100%);
  padding: 10rpx 16rpx;
  border-radius: 25rpx;
  box-shadow: 0 0 18rpx rgba(110, 210, 255, 0.32), inset 0 2rpx 8rpx rgba(255, 255, 255, 0.28);
  border: 2rpx solid rgba(255, 255, 255, 0.26);
}

.supply-drop-btn.disabled {
  background: linear-gradient(135deg, #46606b 0%, #30454e 100%);
  box-shadow: none;
  opacity: 0.58;
  border-color: rgba(255, 255, 255, 0.08);
}

.supply-drop-btn:active {
  transform: scale(0.96);
}

.supply-drop-icon {
  font-size: 22rpx;
  margin-right: 6rpx;
}

.supply-drop-text {
  font-size: 22rpx;
  font-weight: bold;
  color: #052133;
  margin-right: 8rpx;
}

.supply-drop-cost {
  font-size: 20rpx;
  color: #052133;
  font-weight: bold;
}

.commander-btn {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #c9f6ff 0%, #6e84ff 100%);
  padding: 10rpx 16rpx;
  border-radius: 25rpx;
  box-shadow: 0 0 20rpx rgba(120, 160, 255, 0.35), inset 0 2rpx 8rpx rgba(255, 255, 255, 0.25);
  border: 2rpx solid rgba(255, 255, 255, 0.26);
}

.commander-btn.targeting {
  border-color: rgba(220, 255, 255, 0.9);
  box-shadow: 0 0 26rpx rgba(180, 235, 255, 0.62), inset 0 2rpx 8rpx rgba(255, 255, 255, 0.32);
}

.commander-btn.disabled {
  background: linear-gradient(135deg, #4c556d 0%, #353b55 100%);
  box-shadow: none;
  opacity: 0.58;
  border-color: rgba(255, 255, 255, 0.08);
}

.commander-btn:active {
  transform: scale(0.96);
}

.commander-icon {
  font-size: 22rpx;
  margin-right: 6rpx;
}

.commander-text {
  font-size: 22rpx;
  font-weight: bold;
  color: #101a3d;
  margin-right: 8rpx;
}

.commander-cost {
  font-size: 20rpx;
  color: #101a3d;
  font-weight: bold;
}

.summon-btn {
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #ffd700 0%, #ff9500 100%);
  padding: 10rpx 18rpx;
  border-radius: 25rpx;
  box-shadow: 0 0 20rpx rgba(255, 215, 0, 0.5), inset 0 2rpx 8rpx rgba(255, 255, 255, 0.3);
  border: 2rpx solid rgba(255, 255, 255, 0.3);
}

.summon-btn:active {
  transform: scale(0.96);
  box-shadow: 0 0 30rpx rgba(255, 215, 0, 0.8);
}

.summon-btn.disabled {
  background: linear-gradient(135deg, #555 0%, #333 100%);
  box-shadow: none;
  opacity: 0.5;
  border-color: rgba(255, 255, 255, 0.1);
}

.summon-icon {
  font-size: 22rpx;
  margin-right: 6rpx;
}

.summon-text {
  font-size: 22rpx;
  font-weight: bold;
  color: #1a1a00;
  margin-right: 8rpx;
}

.summon-cost {
  font-size: 20rpx;
  color: #1a1a00;
  font-weight: bold;
}

.inventory-hint {
  text-align: center;
  margin-bottom: 4rpx;
}

.inventory-hint text {
  font-size: 18rpx;
  color: rgba(150, 200, 150, 0.7);
}

.inventory-grid {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 6rpx;
}

.inventory-grid.dimmed {
  opacity: 0.2;
  filter: saturate(0.7);
}

.choice-drawer {
  position: absolute;
  inset: 0;
  z-index: 12;
  padding: 28rpx 10rpx 12rpx;
  background: linear-gradient(180deg, rgba(4, 10, 4, 0) 0%, rgba(4, 10, 4, 0.48) 42%, rgba(4, 10, 4, 0.84) 100%);
  display: flex;
  align-items: flex-end;
}

.choice-drawer-panel {
  width: 100%;
  max-height: 330rpx;
  padding: 18rpx 18rpx 16rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(120, 255, 180, 0.22);
  background: linear-gradient(180deg, rgba(10, 30, 12, 0.96) 0%, rgba(12, 24, 12, 0.99) 100%);
  box-shadow: 0 0 28rpx rgba(80, 255, 120, 0.14);
}

.choice-drawer-head {
  margin-bottom: 12rpx;
  padding: 0 4rpx;
}

.choice-drawer-title {
  display: block;
  text-align: center;
  font-size: 26rpx;
  font-weight: 800;
  color: #9bff86;
}

.choice-drawer-subtitle {
  display: block;
  margin-top: 4rpx;
  text-align: center;
  font-size: 19rpx;
  line-height: 1.35;
  color: rgba(244, 250, 230, 0.9);
}

.choice-drawer-hint {
  display: block;
  margin-top: 4rpx;
  text-align: center;
  font-size: 16rpx;
  line-height: 1.35;
  color: rgba(185, 224, 255, 0.7);
}

.choice-drawer-scroll {
  width: 100%;
  white-space: nowrap;
}

.choice-drawer-list {
  display: inline-flex;
  gap: 12rpx;
  padding-right: 4rpx;
}

.choice-drawer-card {
  box-sizing: border-box;
  width: 248rpx;
  min-height: 180rpx;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 10rpx;
  padding: 16rpx 16rpx 18rpx;
  border-radius: 18rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.08);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.06) 0%, rgba(255, 255, 255, 0.02) 100%);
}

.choice-drawer-card:active {
  transform: scale(0.985);
  border-color: rgba(255, 215, 120, 0.55);
}

.choice-drawer-icon {
  flex-shrink: 0;
  font-size: 30rpx;
  line-height: 1;
}

.choice-drawer-copy {
  flex: 1;
  min-width: 0;
  width: 100%;
  display: flex;
  flex-direction: column;
}

.choice-drawer-name {
  width: 100%;
}

.choice-drawer-name text {
  display: block;
  width: 100%;
  font-size: 24rpx;
  font-weight: 800;
  line-height: 1.25;
  color: #fff3c5;
  white-space: normal;
  word-break: break-word;
}

.choice-drawer-desc {
  width: 100%;
  margin-top: 6rpx;
  max-height: 102rpx;
  overflow: hidden;
}

.choice-drawer-desc text {
  display: block;
  width: 100%;
  font-size: 18rpx;
  line-height: 1.4;
  color: rgba(232, 255, 232, 0.74);
  white-space: normal;
  word-break: break-word;
}

.inventory-area.battle-running .fire-flame,
.inventory-area.battle-running .arcane-gem,
.inventory-area.battle-running .lightning-orb,
.inventory-area.battle-running .inventory-slot.merge-target {
  animation: none;
}

.inventory-area.battle-running .inventory-slot {
  box-shadow: inset 0 1rpx 5rpx rgba(0, 0, 0, 0.22);
}

.inventory-slot {
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(135deg, rgba(20, 40, 20, 0.8) 0%, rgba(10, 25, 10, 0.9) 100%);
  border: 2rpx solid rgba(80, 255, 80, 0.25);
  border-radius: 12rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  box-shadow: inset 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
}

.inventory-slot.has-tower {
  border-color: rgba(80, 255, 80, 0.5);
  background: linear-gradient(135deg, rgba(40, 80, 40, 0.4) 0%, rgba(20, 50, 20, 0.6) 100%);
  box-shadow: 0 0 15rpx rgba(80, 255, 80, 0.2), inset 0 2rpx 8rpx rgba(0, 0, 0, 0.2);
}

/* 不同类型塔的边框颜色 */
.inventory-slot.tower-fire {
  border-color: rgba(255, 68, 0, 0.6);
  box-shadow: 0 0 15rpx rgba(255, 68, 0, 0.3);
}

.inventory-slot.tower-ice {
  border-color: rgba(0, 204, 255, 0.6);
  box-shadow: 0 0 15rpx rgba(0, 204, 255, 0.3);
}

.inventory-slot.tower-nature {
  border-color: rgba(68, 255, 68, 0.6);
  box-shadow: 0 0 15rpx rgba(68, 255, 68, 0.3);
}

.inventory-slot.tower-arcane {
  border-color: rgba(170, 68, 255, 0.6);
  box-shadow: 0 0 15rpx rgba(170, 68, 255, 0.3);
}

.inventory-slot.tower-lightning {
  border-color: rgba(255, 255, 0, 0.6);
  box-shadow: 0 0 15rpx rgba(255, 255, 0, 0.3);
}

.inventory-slot.dragging {
  opacity: 0.4;
  transform: scale(0.9);
  border-style: dashed;
}

.inventory-slot.merge-target {
  border-color: #ffd700 !important;
  box-shadow: 0 0 20px rgba(255, 215, 0, 0.8), 0 0 40px rgba(255, 215, 0, 0.4) !important;
  animation: mergeTargetPulse 0.5s ease-in-out infinite;
}

@keyframes mergeTargetPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.inventory-slot.empty {
  border-style: dashed;
  border-color: rgba(80, 255, 80, 0.2);
}

.slot-tower {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.tower-shape {
  width: 100%;
  height: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* ===== 火焰塔 - 熔岩石塔 ===== */
.fire-shape {
  position: relative;
}
.fire-body {
  width: 24rpx;
  height: 30rpx;
  background: linear-gradient(0deg, #441100 0%, #883311 40%, #aa5533 100%);
  clip-path: polygon(10% 100%, 25% 10%, 38% 0%, 62% 0%, 75% 10%, 90% 100%);
  position: absolute;
  bottom: 0rpx;
  border-radius: 2rpx 2rpx 0 0;
}
.fire-flame {
  width: 20rpx;
  height: 24rpx;
  position: absolute;
  top: -2rpx;
  background: radial-gradient(ellipse at 50% 90%, #ffffff 0%, #ffff44 15%, #ff8800 40%, #ff4400 65%, rgba(255, 30, 0, 0) 100%);
  clip-path: polygon(50% 0%, 85% 30%, 75% 60%, 90% 75%, 50% 100%, 10% 75%, 25% 60%, 15% 30%);
  animation: flameFlicker 0.3s ease-in-out infinite alternate;
  filter: brightness(1.1);
}
@keyframes flameFlicker {
  0% { transform: scaleX(0.92) scaleY(1.06); opacity: 0.9; }
  100% { transform: scaleX(1.08) scaleY(0.94); opacity: 1; }
}

/* ===== 寒冰塔 - 冰锥尖塔 ===== */
.ice-shape {
  position: relative;
}
.ice-crystal {
  width: 12rpx;
  height: 10rpx;
  background: linear-gradient(135deg, #003344 0%, #006688 100%);
  position: absolute;
  bottom: 0rpx;
  clip-path: polygon(0% 100%, 15% 0%, 85% 0%, 100% 100%);
}
.ice-shine {
  position: absolute;
  bottom: 8rpx;
  width: 0;
  height: 0;
  border-left: 8rpx solid transparent;
  border-right: 8rpx solid transparent;
  border-bottom: 36rpx solid #33bbdd;
  filter: brightness(1.1);
}
.ice-shine::after {
  content: '';
  position: absolute;
  top: 6rpx;
  left: -4rpx;
  width: 0;
  height: 0;
  border-left: 4rpx solid transparent;
  border-right: 4rpx solid transparent;
  border-bottom: 26rpx solid rgba(200, 240, 255, 0.4);
}

/* ===== 自然塔 - 生命之树 ===== */
.nature-shape {
  position: relative;
}
.nature-trunk {
  width: 8rpx;
  height: 22rpx;
  background: linear-gradient(90deg, #2a1500 0%, #5a3000 50%, #3d2200 100%);
  position: absolute;
  bottom: 0rpx;
  border-radius: 2rpx;
}
.nature-canopy {
  width: 34rpx;
  height: 30rpx;
  position: absolute;
  top: 0rpx;
  background: radial-gradient(circle at 40% 35%, #66ee44 0%, #33aa22 50%, #116600 100%);
  border-radius: 50% 50% 45% 45%;
  box-shadow: inset -4rpx -4rpx 8rpx rgba(0, 40, 0, 0.3), inset 4rpx 4rpx 6rpx rgba(150, 255, 100, 0.15);
}
.nature-canopy::before {
  content: '';
  position: absolute;
  width: 10rpx;
  height: 8rpx;
  background: rgba(180, 255, 120, 0.4);
  border-radius: 50%;
  top: 6rpx;
  left: 8rpx;
}

/* ===== 奥术塔 - 悬浮宝石 ===== */
.arcane-shape {
  position: relative;
}
.arcane-base {
  width: 18rpx;
  height: 14rpx;
  background: linear-gradient(135deg, #555577, #333355, #222233);
  clip-path: polygon(15% 100%, 0% 0%, 100% 0%, 85% 100%);
  position: absolute;
  bottom: 0rpx;
}
.arcane-gem {
  width: 24rpx;
  height: 28rpx;
  background: linear-gradient(135deg, #ee99ff 0%, #aa44dd 40%, #6622aa 70%, #330066 100%);
  clip-path: polygon(50% 0%, 100% 40%, 50% 100%, 0% 40%);
  position: absolute;
  top: 0rpx;
  animation: gemFloat 1.5s ease-in-out infinite alternate;
}
.arcane-gem::after {
  content: '';
  position: absolute;
  width: 8rpx;
  height: 10rpx;
  background: rgba(255, 200, 255, 0.4);
  clip-path: polygon(0% 0%, 100% 50%, 30% 100%);
  top: 6rpx;
  left: 4rpx;
}
@keyframes gemFloat {
  0% { transform: translateY(1rpx); }
  100% { transform: translateY(-3rpx); }
}

/* ===== 闪电塔 - 能量方尖碑 ===== */
.lightning-shape {
  position: relative;
}
.lightning-coil {
  width: 0;
  height: 0;
  border-left: 7rpx solid transparent;
  border-right: 7rpx solid transparent;
  border-bottom: 34rpx solid #555540;
  position: absolute;
  bottom: 0rpx;
}
.lightning-coil::after {
  content: '';
  position: absolute;
  bottom: -34rpx;
  left: -10rpx;
  width: 20rpx;
  height: 10rpx;
  background: linear-gradient(135deg, #2a2a20 0%, #4a4a30 50%, #2a2a20 100%);
  clip-path: polygon(0% 100%, 10% 0%, 90% 0%, 100% 100%);
}
.lightning-orb {
  width: 14rpx;
  height: 14rpx;
  background: radial-gradient(circle at 35% 35%, #ffffff 0%, #ffff88 30%, #ddcc00 70%, rgba(180, 180, 0, 0) 100%);
  border-radius: 50%;
  position: absolute;
  top: 2rpx;
  box-shadow: 0 0 10rpx rgba(255, 255, 0, 0.9), 0 0 20rpx rgba(255, 255, 0, 0.4);
  animation: orbPulse 0.5s ease-in-out infinite alternate;
}
@keyframes orbPulse {
  0% { box-shadow: 0 0 6rpx rgba(255, 255, 0, 0.6); transform: scale(0.95); }
  100% { box-shadow: 0 0 16rpx rgba(255, 255, 0, 1); transform: scale(1.05); }
}

.tower-level-badge {
  position: absolute;
  top: 3rpx;
  right: 3rpx;
  background: linear-gradient(135deg, #ffd700 0%, #ff9500 100%);
  padding: 1rpx 8rpx;
  border-radius: 8rpx;
  box-shadow: 0 2rpx 5rpx rgba(0, 0, 0, 0.3);
}

.tower-level {
  font-size: 16rpx;
  color: #1a1a00;
  font-weight: bold;
}

.tower-name {
  margin-top: 1rpx;
}

.tower-name text {
  font-size: 16rpx;
  color: rgba(255, 255, 255, 0.7);
}

.empty-slot {
  font-size: 36rpx;
  color: rgba(80, 255, 80, 0.25);
}

/* 战前准备 */
.prep-hud {
  padding: 16rpx 18rpx 14rpx;
  background: linear-gradient(180deg, rgba(14, 34, 14, 0.96) 0%, rgba(8, 20, 8, 0.92) 100%);
  border-bottom: 2rpx solid rgba(80, 255, 80, 0.16);
  box-shadow: 0 8rpx 22rpx rgba(0, 0, 0, 0.18);
}

.prep-hud-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16rpx;
}

.prep-copy {
  flex: 1;
  min-width: 0;
}

.prep-hud-eyebrow {
  display: block;
  font-size: 18rpx;
  letter-spacing: 3rpx;
  color: rgba(168, 255, 168, 0.54);
}

.prep-hud-title {
  display: block;
  margin-top: 6rpx;
  font-size: 28rpx;
  font-weight: 800;
  color: #8bff7b;
  line-height: 1.35;
  text-shadow: 0 0 14rpx rgba(120, 255, 120, 0.22);
}

.prep-progress {
  flex-shrink: 0;
  padding: 8rpx 14rpx;
  border-radius: 999rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.08);
  background: rgba(255, 255, 255, 0.04);
}

.prep-progress.ready {
  border-color: rgba(139, 255, 123, 0.42);
  box-shadow: 0 0 18rpx rgba(120, 255, 120, 0.18);
}

.prep-progress text {
  font-size: 20rpx;
  color: rgba(245, 252, 240, 0.84);
  font-weight: 700;
}

.prep-step-line {
  display: flex;
  gap: 10rpx;
  margin-top: 14rpx;
}

.prep-step {
  flex: 1;
  min-width: 0;
  text-align: center;
  padding: 8rpx 0;
  border-radius: 999rpx;
  font-size: 18rpx;
  color: rgba(220, 255, 220, 0.56);
  background: rgba(255, 255, 255, 0.04);
  border: 2rpx solid rgba(255, 255, 255, 0.05);
}

.prep-step.active {
  color: #fff4cf;
  border-color: rgba(255, 215, 120, 0.45);
  background: rgba(255, 215, 120, 0.1);
}

.prep-step.done,
.prep-step.ready {
  color: #9bff86;
  border-color: rgba(139, 255, 123, 0.35);
  background: rgba(139, 255, 123, 0.08);
}

.blessing-quick-list {
  display: flex;
  gap: 10rpx;
  margin-top: 14rpx;
}

.blessing-quick-card {
  flex: 1;
  min-width: 0;
  padding: 14rpx 14rpx;
  border-radius: 18rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.08);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(255, 255, 255, 0.02) 100%);
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.blessing-quick-list.compact .blessing-quick-card {
  align-items: center;
  justify-content: center;
  padding: 12rpx 10rpx;
}

.blessing-quick-card.selected {
  border-color: rgba(255, 215, 120, 0.75);
  box-shadow: 0 0 20rpx rgba(255, 215, 120, 0.16);
  background: linear-gradient(135deg, rgba(255, 214, 94, 0.15) 0%, rgba(255, 168, 42, 0.05) 100%);
}

.blessing-quick-icon {
  font-size: 28rpx;
}

.blessing-quick-name {
  display: block;
  font-size: 22rpx;
  font-weight: 700;
  color: #ffffff;
}

.blessing-quick-list.compact .blessing-quick-name {
  font-size: 20rpx;
  text-align: center;
}

.blessing-quick-subtitle {
  display: block;
  font-size: 17rpx;
  line-height: 1.4;
  color: rgba(255, 240, 200, 0.72);
}

.prep-footer {
  margin-top: 14rpx;
}

.prep-footer.compact {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.prep-copy {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.prep-footnote {
  display: block;
  margin-bottom: 14rpx;
  text-align: center;
  color: rgba(255, 255, 255, 0.68);
  font-size: 20rpx;
}

.prep-footer.compact .prep-footnote {
  margin-bottom: 0;
  text-align: left;
  line-height: 1.42;
}

.prep-footnote.ready {
  color: #9bff86;
}

.prep-footnote-secondary {
  display: block;
  margin-top: 0;
  color: rgba(255, 236, 176, 0.9);
  font-size: 17rpx;
  line-height: 1.42;
}

.prep-start-btn {
  width: 204rpx;
  flex-shrink: 0;
  height: 74rpx;
  margin-bottom: 0;
  font-size: 26rpx;
}

/* 遮罩和面板 */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(5, 15, 5, 0.95);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
}

.panel {
  width: 85%;
  max-width: 580rpx;
  padding: 55rpx 45rpx;
  background: linear-gradient(135deg, rgba(40, 80, 40, 0.3) 0%, rgba(20, 50, 20, 0.4) 100%);
  border: 3rpx solid rgba(80, 255, 80, 0.5);
  border-radius: 35rpx;
  box-shadow: 0 0 50rpx rgba(80, 255, 80, 0.2), inset 0 2rpx 15rpx rgba(255, 255, 255, 0.05);
}

.supply-panel {
  width: 88%;
  max-width: 620rpx;
  padding: 48rpx 34rpx 34rpx;
}

.supply-subtitle {
  display: block;
  margin-top: -18rpx;
  margin-bottom: 12rpx;
  text-align: center;
  font-size: 24rpx;
  color: rgba(220, 255, 220, 0.72);
}

.supply-subhint {
  display: block;
  margin-bottom: 24rpx;
  text-align: center;
  font-size: 20rpx;
  line-height: 1.5;
  color: rgba(195, 232, 255, 0.74);
}

.supply-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.supply-card {
  padding: 24rpx 22rpx;
  border-radius: 24rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.08);
  background: linear-gradient(135deg, rgba(255, 255, 255, 0.06) 0%, rgba(255, 255, 255, 0.02) 100%);
}

.supply-card:active {
  transform: scale(0.98);
  border-color: rgba(255, 215, 120, 0.55);
}

.supply-icon {
  display: block;
  font-size: 42rpx;
}

.supply-name {
  display: block;
  margin-top: 10rpx;
  font-size: 30rpx;
  font-weight: 800;
  color: #fff5c8;
}

.supply-desc {
  display: block;
  margin-top: 8rpx;
  font-size: 22rpx;
  line-height: 1.5;
  color: rgba(230, 255, 230, 0.72);
}

.panel-title {
  display: block;
  text-align: center;
  font-size: 52rpx;
  font-weight: bold;
  color: #50ff50;
  margin-bottom: 40rpx;
  text-shadow: 0 0 25rpx rgba(80, 255, 80, 0.8), 0 3rpx 6rpx rgba(0, 0, 0, 0.5);
}

.panel-title.gameover {
  color: #ff6666;
  text-shadow: 0 0 25rpx rgba(255, 102, 102, 0.8), 0 3rpx 6rpx rgba(0, 0, 0, 0.5);
}

.panel-stats {
  margin-bottom: 40rpx;
}

.panel-stat {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 18rpx 25rpx;
  background: linear-gradient(135deg, rgba(0, 0, 0, 0.4) 0%, rgba(0, 0, 0, 0.2) 100%);
  border-radius: 15rpx;
  margin-bottom: 18rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
}

.panel-stat-label {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.7);
}

.panel-stat-value {
  font-size: 34rpx;
  font-weight: bold;
  color: #fff;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.5);
}

.panel-stat-value.big {
  font-size: 46rpx;
  color: #ffd700;
  text-shadow: 0 0 15rpx rgba(255, 215, 0, 0.5);
}

.panel-stat-value.gold-text {
  color: #ffd700;
  text-shadow: 0 0 15rpx rgba(255, 215, 0, 0.5);
}

.record {
  text-align: center;
  padding: 22rpx;
  background: linear-gradient(135deg, rgba(255, 170, 0, 0.2) 0%, rgba(255, 200, 0, 0.15) 100%);
  border-radius: 15rpx;
  margin-top: 12rpx;
  border: 2rpx solid rgba(255, 170, 0, 0.4);
}

.record text {
  font-size: 40rpx;
  color: #ffaa00;
  animation: recordPulse 1s ease-in-out infinite;
  text-shadow: 0 0 15rpx rgba(255, 170, 0, 0.6);
}

@keyframes recordPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

.btn {
  width: 100%;
  height: 95rpx;
  border-radius: 20rpx;
  font-size: 32rpx;
  font-weight: bold;
  margin-bottom: 22rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
}

.btn.primary {
  background: linear-gradient(135deg, #50ff50 0%, #20aa20 100%);
  color: #0d1f0d;
  box-shadow: 0 0 25rpx rgba(80, 255, 80, 0.5), inset 0 2rpx 8rpx rgba(255, 255, 255, 0.2);
}

.btn.primary.disabled,
.btn.primary[disabled] {
  background: linear-gradient(135deg, rgba(97, 116, 97, 0.9) 0%, rgba(66, 80, 66, 0.92) 100%);
  color: rgba(226, 236, 226, 0.62);
  box-shadow: none;
}

.btn.primary:active {
  background: linear-gradient(135deg, #40dd40 0%, #188818 100%);
  box-shadow: 0 0 35rpx rgba(80, 255, 80, 0.7);
}

.btn.secondary {
  background: linear-gradient(135deg, rgba(80, 80, 80, 0.4) 0%, rgba(50, 50, 50, 0.5) 100%);
  border: 2rpx solid rgba(255, 255, 255, 0.25);
  color: rgba(255, 255, 255, 0.85);
}

.btn.secondary:active {
  background: rgba(100, 100, 100, 0.5);
}
